<link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-menu-drawer.css' | asset_url }}" media="print" onload="this.media='all'">
<link
  rel="stylesheet"
  href="{{ 'component-cart-notification.css' | asset_url }}"
  media="print"
  onload="this.media='all'"
>

{%- if settings.predictive_search_enabled -%}
  <link rel="stylesheet" href="{{ 'component-price.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}

{%- if section.settings.menu_type_desktop == 'mega' -%}
  <link rel="stylesheet" href="{{ 'component-mega-menu.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}

<style>
    header-drawer {
      justify-self: start;
      margin-left: -1.2rem;
    }
    .neg-zindex {
      {% comment %} z-index:-1; {% endcomment %}
    }
    .bg-card {
      background-color: #232422!important;
    }

    {%- if section.settings.sticky_header_type == 'reduce-logo-size' -%}
      .scrolled-past-header .header__heading-logo-wrapper {
        width: 75%;
      }
    {%- endif -%}

    {%- if section.settings.menu_type_desktop != "drawer" -%}
      @media screen and (min-width: 990px) {
        header-drawer {
          display: none;
        }
      }
    {%- endif -%}

    .menu-drawer-container {
      display: flex;
    }

    .list-menu {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .list-menu--inline {
      display: inline-flex;
      flex-wrap: wrap;
    }

    summary.list-menu__item {
      padding-right: 2.7rem;
    }

    .list-menu__item {
      display: flex;
      align-items: center;
      line-height: calc(1 + 0.3 / var(--font-body-scale));
    }

    .list-menu__item--link {
      text-decoration: none;
      padding-bottom: 1rem;
      padding-top: 1rem;
      line-height: calc(1 + 0.8 / var(--font-body-scale));
    }

    @media screen and (min-width: 750px) {
      .list-menu__item--link {
        padding-bottom: 0.5rem;
        padding-top: 0.5rem;
      }
    }

    /* Search overlay animations */
    .fade-in-from-header {
      animation: fadeInFromHeader 0.3s ease-out forwards;
    }

    @keyframes fadeInFromHeader {
      from {
        opacity: 0;
        transform: translateX(-50%) translateY(-20px) scale(0.95);
      }
      to {
        opacity: 1;
        transform: translateX(-50%) translateY(0) scale(1);
      }
    }

    .bg-search {
      background-color: rgba(var(--color-base-accent-1), 0.08);
    }

    .bg-card {
      background-color: rgb(var(--color-base-background-1));
    }
    .border-border {
      border-color: var(--border);
  }
    .text-muted-foreground {
      color: rgba(var(--color-base-text), 0.6);
    }
  {% comment %}
    .bg-background {
      background-color: '));
    } {% endcomment %}

    .focus\:ring-accent:focus {
      --tw-ring-color: rgb(var(--color-base-accent-1));
    }
</style>

{%- style -%}
  /* .header {
    padding: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px 3rem {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px 3rem;
  } */

  .section-header {
    position: sticky; /* This is for fixing a Safari z-index issue. PR #2147 */
    margin-bottom: {{ section.settings.margin_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-header {
      margin-bottom: {{ section.settings.margin_bottom }}px;
    }
  }

  @media screen and (min-width: 990px) {
    /* .header {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    } */
  }
{%- endstyle -%}

<script src="{{ 'cart-notification.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'chl-custom-js.js' | asset_url }}" defer="defer"></script>

{%- liquid
  for block in section.blocks
    if block.type == '@app'
      assign has_app_block = true
    endif
  endfor
-%}

{% liquid
  assign header_tag = 'div'

  if section.settings.sticky_header_type != 'none'
    assign header_tag = 'sticky-header'
  endif
%}

<{{ header_tag }}
  {% if header_tag == 'sticky-header' %}
    data-sticky-type="{{ section.settings.sticky_header_type }}"
  {% endif %}
  class="header-wrapper color-{{ section.settings.color_scheme }} gradient{% if section.settings.show_line_separator %} header-wrapper--border-bottom{% endif %}"
>
  {%- liquid
    assign social_links = false
    assign localization_forms = false

    if settings.social_twitter_link != blank or settings.social_facebook_link != blank or settings.social_pinterest_link != blank or settings.social_instagram_link != blank or settings.social_youtube_link != blank or settings.social_vimeo_link != blank or settings.social_tiktok_link != blank or settings.social_tumblr_link != blank or settings.social_snapchat_link != blank
      assign social_links = true
    endif

    if localization.available_countries.size > 1 and section.settings.enable_country_selector or section.settings.enable_language_selector and localization.available_languages.size > 1
      assign localization_forms = true
    endif
  -%}
  <header class="sticky top-0 z-[60] bg-brand-black text-white backdrop-blur-sm border-b border-border">
    <div class="container mx-auto px-4 relative">
      <div class="flex items-center justify-between h-24">
        <a href="/" class="flex items-center">
          <div class="text-2xl font-bold tracking-tight text-white hover:text-brand-orange transition-colors p-2">
            <img
              elementtiming="nbf-header-logo-desktop"
              src="{{ 'logo.png' | asset_url }}"
              alt="Whiffed Aromas"
              style="display:block"
              fetchpriority="high"
              loading="eager"
              decoding="async"
              data-loader="raw"
              width="100"
              height="100"
            >
          </div>
        </a>
        <nav class="hidden lg:flex items-center gap-8">
          <a href="/" class="text-sm font-heading font-medium text-white transition-colors">HOME</a>
          <div class="relative group">
            <button class="text-sm font-heading font-medium text-white transition-colors flex items-center gap-1">
              SHOP
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="lucide lucide-chevron-down h-3 w-3 transition-transform group-hover:rotate-180"
              >
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>

            <!-- Mega Menu -->
            <div
              class="mega-menu-container fixed left-1/2 -translate-x-1/2 pt-4 transition-all duration-300 opacity-0 invisible translate-y-2 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 z-[9999]"
              style="top: 96px; transform: translateX(20%);"
            >
              <div class="bg-card border border-border rounded-2xl shadow-2xl overflow-hidden">
                <div class="p-4 md:p-8 grid grid-cols-1 md:grid-cols-[repeat(auto-fit,minmax(200px,1fr))] gap-4 md:gap-8 w-[calc(100vw-2rem)] md:w-auto md:min-w-[700px] max-w-[calc(100vw-2rem)] md:max-w-6xl">
                  <!-- Hotel Collection -->
                  <div class="space-y-4">
                    {% assign hotel_title = section.settings.hotel_collection_title | default: 'Hotel Collection' %}
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
                      {{ hotel_title }}
                    </h3>
                    <ul class="space-y-3">
                      {% assign hotel_links = section.settings.hotel_collection_links
                        | default: 'Calm Down|/collections/calm-down,Zest & The City|/collections/zest-the-city,Peach Tease|/collections/peach-tease,Mystic Nights|/collections/mystic-nights,Blooming Flirt|/collections/blooming-flirt,Wet Bar|/collections/wet-bar'
                      %}
                      {% assign hotel_items = hotel_links | split: ',' %}
                      {% for item in hotel_items %}
                        {% assign link_parts = item | split: '|' %}
                        {% if link_parts.size == 2 %}
                          <li>
                            <a
                              href="{{ link_parts[1] | strip }}"
                              class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                            >
                              {{- link_parts[0] | strip }}
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="14"
                                height="14"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                              >
                                <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                              </svg>
                            </a>
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </div>

                  <!-- Gilded Exotics -->
                  <div class="space-y-4">
                    {% assign gilded_title = section.settings.gilded_collection_title | default: 'Gilded Exotics' %}
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
                      {{ gilded_title }}
                    </h3>
                    <ul class="space-y-3">
                      {% assign gilded_links = section.settings.gilded_collection_links
                        | default: 'On Par|/collections/on-par,Sweet Juliet|/collections/sweet-juliet,Juicy Theory|/collections/juicy-theory,Portable Units|/collections/portable-diffusers'
                      %}
                      {% assign gilded_items = gilded_links | split: ',' %}
                      {% for item in gilded_items %}
                        {% assign link_parts = item | split: '|' %}
                        {% if link_parts.size == 2 %}
                          <li>
                            <a
                              href="{{ link_parts[1] | strip }}"
                              class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                            >
                              {{- link_parts[0] | strip }}
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="14"
                                height="14"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                              >
                                <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                              </svg>
                            </a>
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </div>

                  <!-- Popular Collections -->
                  <div class="space-y-4">
                    {% assign popular_title = section.settings.popular_collection_title
                      | default: 'Popular Collections'
                    %}
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
                      {{ popular_title }}
                    </h3>
                    <ul class="space-y-3">
                      {% assign popular_links = section.settings.popular_collection_links
                        | default: 'Best Sellers|/collections/best-sellers,New Arrivals|/collections/new-arrivals,Bundle Deals|/collections/bundles-deals,Starter Kits|/collections/starter-kits'
                      %}
                      {% assign popular_items = popular_links | split: ',' %}
                      {% for item in popular_items %}
                        {% assign link_parts = item | split: '|' %}
                        {% if link_parts.size == 2 %}
                          <li>
                            <a
                              href="{{ link_parts[1] | strip }}"
                              class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                            >
                              {{- link_parts[0] | strip }}
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="14"
                                height="14"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                              >
                                <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                              </svg>
                            </a>
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </div>

                  <!-- Featured Product Card 1 -->
                  {% assign card1_url = section.settings.featured_card1_url | default: '/collections/best-sellers' %}
                  {% assign card1_category = section.settings.featured_card1_category | default: 'Best Sellers' %}
                  {% assign card1_title = section.settings.featured_card1_title | default: 'Top Scents' %}
                  {% assign card1_description = section.settings.featured_card1_description
                    | default: 'Most loved fragrances'
                  %}
                  <a
                    href="{{ card1_url }}"
                    class="group relative overflow-hidden rounded-xl bg-secondary hover:shadow-lg transition-all"
                  >
                    <div class="aspect-[3/4] relative overflow-hidden bg-gradient-to-br from-purple-50 to-purple-100">
                      <div class="absolute inset-0 flex flex-col">
                        <div class="flex-1 flex items-center justify-center p-6">
                          <div class="text-center">
                            <div class="w-16 h-16 mx-auto mb-4 bg-purple-500 rounded-full flex items-center justify-center">
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="32"
                                height="32"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="white"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                              >
                                <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
                              </svg>
                            </div>
                          </div>
                        </div>
                        <div class="p-6 bg-gradient-to-t from-purple-900/90 to-transparent text-white">
                          <p class="text-xs uppercase tracking-wider mb-1 opacity-90">{{ card1_category }}</p>
                          <h4 class="text-lg font-semibold mb-2">{{ card1_title }}</h4>
                          <p class="text-sm opacity-90">{{ card1_description }}</p>
                        </div>
                      </div>
                    </div>
                  </a>

                  <!-- Featured Product Card 2 -->
                  {% assign card2_url = section.settings.featured_card2_url | default: '/collections/bundles-deals' %}
                  {% assign card2_category = section.settings.featured_card2_category | default: 'Save More' %}
                  {% assign card2_title = section.settings.featured_card2_title | default: 'Bundle Deals' %}
                  {% assign card2_description = section.settings.featured_card2_description
                    | default: 'Complete scent packages'
                  %}
                  <a
                    href="{{ card2_url }}"
                    class="group relative overflow-hidden rounded-xl bg-secondary hover:shadow-lg transition-all"
                  >
                    <div class="aspect-[3/4] relative overflow-hidden bg-gradient-to-br from-orange-50 to-orange-100">
                      <div class="absolute inset-0 flex flex-col">
                        <div class="flex-1 flex items-center justify-center p-6">
                          <div class="text-center">
                            <div class="w-16 h-16 mx-auto mb-4 bg-orange-500 rounded-full flex items-center justify-center">
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="32"
                                height="32"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="white"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                              >
                                <path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"></path>
                                <rect width="8" height="4" x="8" y="2" rx="1" ry="1"></rect>
                                <path d="m9 14 2 2 4-4"></path>
                              </svg>
                            </div>
                          </div>
                        </div>
                        <div class="p-6 bg-gradient-to-t from-orange-900/90 to-transparent text-white">
                          <p class="text-xs uppercase tracking-wider mb-1 opacity-90">{{ card2_category }}</p>
                          <h4 class="text-lg font-semibold mb-2">{{ card2_title }}</h4>
                          <p class="text-sm opacity-90">{{ card2_description }}</p>
                        </div>
                      </div>
                    </div>
                  </a>
                </div>
              </div>
            </div>
          </div>

          <!-- BUNDLES Mega Menu -->
          <div class="relative group">
            <button class="text-sm font-heading font-medium text-white transition-colors flex items-center gap-1">
              BUNDLES
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="lucide lucide-chevron-down h-3 w-3 transition-transform group-hover:rotate-180"
              >
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>

            <div class="mega-menu-container fixed left-1/2 -translate-x-1/2 pt-4 transition-all duration-300 opacity-0 invisible translate-y-2 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 z-[9999]" style="top: 96px;">
              <div class="bg-card border border-border rounded-2xl shadow-2xl overflow-hidden">
                <div class="p-6 grid grid-cols-2 gap-6 w-[400px]">
                  <!-- Bundle Types -->
                  <div class="space-y-4">
                    {% assign bundle_types_title = section.settings.bundle_types_title | default: 'Bundle Types' %}
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
                      {{ bundle_types_title }}
                    </h3>
                    <ul class="space-y-3">
                      {% assign bundle_types_links = section.settings.bundle_types_links
                        | default: 'Starter Bundles|/collections/starter-bundles,Home Bundles|/collections/home-bundles,Car Bundles|/collections/car-bundles,Gift Bundles|/collections/gift-bundles'
                      %}
                      {% assign bundle_types_items = bundle_types_links | split: ',' %}
                      {% for item in bundle_types_items %}
                        {% assign link_parts = item | split: '|' %}
                        {% if link_parts.size == 2 %}
                          <li>
                            <a
                              href="{{ link_parts[1] | strip }}"
                              class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                            >
                              {{- link_parts[0] | strip }}
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="14"
                                height="14"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                              >
                                <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                              </svg>
                            </a>
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </div>

                  <!-- Special Offers -->
                  <div class="space-y-4">
                    {% assign special_offers_title = section.settings.special_offers_title
                      | default: 'Special Offers'
                    %}
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
                      {{ special_offers_title }}
                    </h3>
                    <ul class="space-y-3">
                      {% assign special_offers_links = section.settings.special_offers_links
                        | default: 'Flash Deals|/collections/flash-deals,Seasonal Bundles|/collections/seasonal-bundles,Bulk Deals|/collections/bulk-deals,Subscription Deals|/collections/subscription-deals'
                      %}
                      {% assign special_offers_items = special_offers_links | split: ',' %}
                      {% for item in special_offers_items %}
                        {% assign link_parts = item | split: '|' %}
                        {% if link_parts.size == 2 %}
                          <li>
                            <a
                              href="{{ link_parts[1] | strip }}"
                              class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                            >
                              {{- link_parts[0] | strip }}
                              <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="14"
                                height="14"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                              >
                                <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                              </svg>
                            </a>
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- SCENTS Mega Menu -->
          <div class="relative group">
            <button class="text-sm font-heading font-medium text-white transition-colors flex items-center gap-1">
              SCENTS
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="lucide lucide-chevron-down h-3 w-3 transition-transform group-hover:rotate-180"
              >
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>

            <div class="mega-menu-container fixed left-1/2 -translate-x-1/2 pt-4 transition-all duration-300 opacity-0 invisible translate-y-2 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 z-[9999]" style="top: 96px;">
              <div class="bg-card border border-border rounded-2xl shadow-2xl overflow-hidden">
                <div class="p-6 grid grid-cols-3 gap-6 w-[600px]">
                  <div class="space-y-4">
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Fresh & Clean</h3>
                    <ul class="space-y-3">
                      <li>
                        <a
                          href="/collections/citrus-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Citrus
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/ocean-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Ocean Breeze
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/mint-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Mint & Eucalyptus
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                    </ul>
                  </div>
                  <div class="space-y-4">
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Warm & Cozy</h3>
                    <ul class="space-y-3">
                      <li>
                        <a
                          href="/collections/vanilla-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Vanilla
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/woody-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Woody
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/spice-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Spice & Amber
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                    </ul>
                  </div>
                  <div class="space-y-4">
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Floral & Sweet</h3>
                    <ul class="space-y-3">
                      <li>
                        <a
                          href="/collections/floral-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Floral
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/lavender-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Lavender
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/fruity-scents"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Fruity & Sweet
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                    </ul>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- MACHINES Mega Menu -->
          <div class="relative group">
            <button class="text-sm font-heading font-medium text-white transition-colors flex items-center gap-1">
              MACHINES
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="lucide lucide-chevron-down h-3 w-3 transition-transform group-hover:rotate-180"
              >
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>

            <div class="mega-menu-container fixed left-1/2 -translate-x-1/2 pt-4 transition-all duration-300 opacity-0 invisible translate-y-2 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 z-[9999]" style="top: 96px;">
              <div class="bg-card border border-border rounded-2xl shadow-2xl overflow-hidden">
                <div class="p-6 grid grid-cols-2 gap-6 w-[500px]">
                  <div class="space-y-4">
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">By Location</h3>
                    <ul class="space-y-3">
                      <li>
                        <a
                          href="/collections/home-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Home Diffusers
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/car-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Car Diffusers
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/office-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Office Diffusers
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/commercial-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Commercial Units
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                    </ul>
                  </div>
                  <div class="space-y-4">
                    <h3 class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">By Size</h3>
                    <ul class="space-y-3">
                      <li>
                        <a
                          href="/collections/small-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Small (100-500 sq ft)
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/medium-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Medium (500-1500 sq ft)
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/large-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Large (1500+ sq ft)
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                      <li>
                        <a
                          href="/collections/portable-diffusers"
                          class="text-sm hover:text-accent transition-colors inline-flex items-center gap-2 group"
                          >Portable Units
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all"
                          >
                            <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
                          </svg>
                        </a>
                      </li>
                    </ul>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <a href="/company" class="text-sm font-heading font-medium text-white transition-colors"
            >COMPANY</a
          >
          <a href="/pages/media" class="text-sm font-heading font-medium text-white transition-colors"
            >MEDIA</a
          >
        </nav>
        <div class="flex items-center gap-4">
          <button
            id="search-collapsed"
            data-slot="button"
            class="items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 [&amp;_svg]:pointer-events-none [&amp;_svg:not([class*='size-'])]:size-4 shrink-0 [&amp;_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive size-9 hidden md:flex"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="lucide lucide-search h-5 w-5"
            >
              <circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path>
            </svg>
          </button>
          <a
            href="/account"
            class="hidden md:block text-sm font-heading font-medium text-white transition-colors"
            >LOGIN</a
          >
          <div class="hidden md:block">
            <button
              id="cart-trigger"
              data-slot="button"
              class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 [&amp;_svg]:pointer-events-none [&amp;_svg:not([class*='size-'])]:size-4 shrink-0 [&amp;_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive size-9 relative"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="lucide lucide-shopping-cart h-5 w-5"
              >
                <circle cx="8" cy="21" r="1"></circle><circle cx="19" cy="21" r="1"></circle><path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"></path>
              </svg>
              <span
                id="cart-count"
                data-slot="badge"
                class="border font-medium whitespace-nowrap shrink-0 [&amp;&gt;svg]:size-3 gap-1 [&amp;&gt;svg]:pointer-events-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive transition-[color,box-shadow] overflow-hidden border-transparent bg-primary text-primary-foreground [a&amp;]:hover:bg-primary/90 absolute -top-1 -right-1 h-5 w-5 rounded-full p-0 flex items-center justify-center text-xs hidden"
                >0</span
              >
            </button>
          </div>
          <button
            id="mobile-menu-trigger"
            class="lg:hidden flex items-center justify-center size-9 hover:bg-accent rounded-md transition-colors"
          >
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </header>

  <!-- Cart Drawer -->
  <div
    id="cart-drawer"
    class="fixed inset-0 bg-black/50 backdrop-blur-sm z-[60] opacity-0 invisible transition-all duration-300"
  >
    <div class="absolute right-0 top-0 h-full w-full max-w-md bg-background transform translate-x-full transition-transform duration-300">
      <div class="flex flex-col h-full">
        <!-- Header -->
        <div class="flex items-center justify-between p-4 border-b border-border">
          <h2 class="text-lg font-semibold">Shopping Cart</h2>
          <button id="cart-drawer-close" class="p-2 hover:bg-accent rounded-lg transition-colors">
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>

        <!-- Cart Items -->
        <div class="flex-1 overflow-y-auto" id="cart-items-container">
          <div id="cart-empty" class="p-6 text-center">
            <svg
              class="w-16 h-16 mx-auto mb-4 text-muted-foreground"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-2.5 5M7 13l2.5 5m6-5v6a2 2 0 01-2 2H9a2 2 0 01-2-2v-6m8 0V9a2 2 0 00-2-2H9a2 2 0 00-2 2v4.01"></path>
            </svg>
            <p class="text-muted-foreground mb-4">Your cart is empty</p>
            <button
              id="continue-shopping"
              class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all h-9 px-4 py-2 bg-primary hover:bg-primary/90 text-primary-foreground"
            >
              Continue Shopping
            </button>
          </div>

          <div id="cart-items" class="hidden">
            <!-- Cart items will be dynamically loaded here -->
          </div>
        </div>

        <!-- Footer -->
        <div id="cart-footer" class="hidden border-t border-border p-4 space-y-4">
          <div class="flex items-center justify-between text-lg font-semibold">
            <span>Total:</span>
            <span id="cart-total">$0.00</span>
          </div>
          <div class="space-y-2">
            <button
              id="view-cart"
              class="w-full inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all h-9 px-4 py-2 border border-input bg-background hover:bg-accent hover:text-accent-foreground"
            >
              View Cart
            </button>
            <button
              id="checkout-btn"
              class="w-full inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all h-9 px-4 py-2 bg-primary hover:bg-primary/90 text-primary-foreground"
            >
              Checkout
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu"
    class="fixed inset-0 bg-black/50 backdrop-blur-sm z-[60] opacity-0 invisible transition-all duration-300 lg:hidden"
  >
    <div class="absolute right-0 top-0 h-full w-full max-w-sm bg-background transform translate-x-full transition-transform duration-300">
      <div class="flex flex-col h-full">
        <!-- Header -->
        <div class="flex items-center justify-between p-4 border-b border-border">
          <h2 class="text-lg font-semibold">Menu</h2>
          <div class="flex items-center gap-2">
            <a href="/account" class="p-2 hover:bg-accent rounded-lg transition-colors">
              <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
            </a>
            <button class="p-2 hover:bg-accent rounded-lg transition-colors">
              <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-2.5 5M7 13l2.5 5m6-5v6a2 2 0 01-2 2H9a2 2 0 01-2-2v-6m8 0V9a2 2 0 00-2-2H9a2 2 0 00-2 2v4.01"></path>
              </svg>
            </button>
            <button id="mobile-menu-close" class="p-2 hover:bg-accent rounded-lg transition-colors">
              <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
            </button>
          </div>
        </div>

        <!-- Navigation -->
        <div class="flex-1 overflow-y-auto">
          <div class="p-4">
            <h3 class="text-xs font-semibold text-muted-foreground uppercase tracking-wider mb-4">EXPLORE</h3>

            <div class="space-y-1">
              <button
                class="mobile-menu-item w-full flex items-center justify-between p-3 hover:bg-accent rounded-lg transition-colors text-left"
                data-target="home-diffusers"
              >
                <span class="font-medium">Home Diffusers</span>
                <svg class="h-4 w-4 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>

              <button
                class="mobile-menu-item w-full flex items-center justify-between p-3 hover:bg-accent rounded-lg transition-colors text-left"
                data-target="car-diffusers"
              >
                <span class="font-medium">Car Diffusers</span>
                <svg class="h-4 w-4 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>

              <button
                class="mobile-menu-item w-full flex items-center justify-between p-3 hover:bg-accent rounded-lg transition-colors text-left"
                data-target="scent-collections"
              >
                <span class="font-medium">Scent Collections</span>
                <svg class="h-4 w-4 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>

              <button
                class="mobile-menu-item w-full flex items-center justify-between p-3 hover:bg-accent rounded-lg transition-colors text-left"
                data-target="bundles"
              >
                <span class="font-medium">Bundles & Deals</span>
                <svg class="h-4 w-4 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>

              <a
                href="/pages/about"
                class="w-full flex items-center justify-between p-3 hover:bg-accent rounded-lg transition-colors text-left"
              >
                <span class="font-medium">About Whiffed</span>
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>

              <a
                href="/pages/media"
                class="w-full flex items-center justify-between p-3 hover:bg-accent rounded-lg transition-colors text-left"
              >
                <span class="font-medium">Press & Media</span>
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>

          <!-- Featured Products -->
          <div class="p-4 border-t border-border">
            <h3 class="text-xs font-semibold text-muted-foreground uppercase tracking-wider mb-4">FEATURED PRODUCTS</h3>

            <div class="space-y-3">
              <div class="flex items-center gap-3 p-2 hover:bg-accent rounded-lg transition-colors">
                <div class="w-12 h-12 bg-muted rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v18m9-9H3"></path>
                  </svg>
                </div>
                <div class="flex-1">
                  <h4 class="font-medium text-sm">Premium Home Diffuser</h4>
                  <p class="text-xs text-muted-foreground">$129.99</p>
                </div>
              </div>

              <div class="flex items-center gap-3 p-2 hover:bg-accent rounded-lg transition-colors">
                <div class="w-12 h-12 bg-muted rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v18m9-9H3"></path>
                  </svg>
                </div>
                <div class="flex-1">
                  <h4 class="font-medium text-sm">Car Scent Bundle</h4>
                  <p class="text-xs text-muted-foreground">$49.99</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Search Overlay -->
  <div
    id="search-overlay"
    class="fixed inset-0 bg-foreground/20 backdrop-blur-sm animate-in fade-in duration-200 hidden"
    style="z-index: 1"
  ></div>

  <div
    id="search-modal"
    class="fixed left-1/2 w-[calc(100vw-2rem)] max-w-3xl animate-in duration-300 hidden"
    style="z-index: 9999; top: 10rem;"
  >
    <div class="bg-card border border-border rounded-2xl shadow-2xl overflow-hidden">
      <div class="p-6 border-b border-border">
        <div class="relative">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground"
          >
            <circle cx="11" cy="11" r="8"></circle>
            <path d="m21 21-4.3-4.3"></path>
          </svg>
          <input
            id="search-modal-input"
            placeholder="Search products, collections, and more..."
            class="w-full !pl-12 !pr-12 !py-4 bg-background rounded-xl border border-border focus:outline-none focus:ring-2 focus:ring-accent text-base"
            type="text"
            value=""
          >
          <button
            id="search-modal-close"
            class="absolute right-4 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M18 6 6 18"></path>
              <path d="m6 6 12 12"></path>
            </svg>
          </button>
        </div>
      </div>
      <div class="max-h-[60vh] overflow-y-auto">
        <div id="search-modal-results" class="hidden">
          <!-- Search results will be populated here -->
        </div>
        <div id="search-modal-popular" class="p-6">
          <h3 class="text-xs uppercase tracking-wider text-muted-foreground !mb-4">Popular Products</h3>
          <div class="flex flex-wrap gap-2">
            {% for product in collections['featured-products'].products limit: 5 %}
              <a
                href="{{ product.url }}"
                class="!px-4 !py-2 bg-search rounded-full text-sm !hover:bg-accent !hover:text-accent-foreground transition-colors"
              >
                {{ product.title }}
              </a>
            {% endfor %}
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- JavaScript -->
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      console.log('Mobile menu script loaded');

      // Mobile Menu
      const mobileMenuTrigger = document.getElementById('mobile-menu-trigger');
      const mobileMenu = document.getElementById('mobile-menu');
      const mobileMenuClose = document.getElementById('mobile-menu-close');

      console.log('Mobile menu elements:', { mobileMenuTrigger, mobileMenu, mobileMenuClose });

      if (mobileMenuTrigger && mobileMenu) {
        mobileMenuTrigger.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          console.log('Mobile menu trigger clicked');

          // Show the menu
          mobileMenu.classList.remove('opacity-0', 'invisible');

          // Find the sliding panel and show it
          const slidePanel = mobileMenu.querySelector('div.transform');
          if (slidePanel) {
            console.log('Sliding panel found, removing translate-x-full');
            // Use setTimeout to ensure the backdrop is visible first
            setTimeout(() => {
              slidePanel.classList.remove('translate-x-full');
            }, 10);
          } else {
            console.log('Slide panel not found');
          }
        });
      } else {
        console.log('Mobile menu trigger or menu not found');
      }

      if (mobileMenuClose && mobileMenu) {
        mobileMenuClose.addEventListener('click', (e) => {
          e.preventDefault();
          console.log('Mobile menu close clicked');

          const slidePanel = mobileMenu.querySelector('div.transform');
          if (slidePanel) {
            slidePanel.classList.add('translate-x-full');
          }

          // Hide after animation
          setTimeout(() => {
            mobileMenu.classList.add('opacity-0', 'invisible');
          }, 300);
        });
      }

      if (mobileMenu) {
        mobileMenu.addEventListener('click', (e) => {
          if (e.target === mobileMenu) {
            console.log('Clicked outside mobile menu');
            const slidePanel = mobileMenu.querySelector('div.transform');
            if (slidePanel) {
              slidePanel.classList.add('translate-x-full');
            }

            setTimeout(() => {
              mobileMenu.classList.add('opacity-0', 'invisible');
            }, 300);
          }
        });
      }

      // Cart Drawer functionality
      const cartTrigger = document.getElementById('cart-trigger');
      const cartDrawer = document.getElementById('cart-drawer');
      const cartDrawerClose = document.getElementById('cart-drawer-close');
      const cartCount = document.getElementById('cart-count');
      const cartItemsContainer = document.getElementById('cart-items');
      const cartEmpty = document.getElementById('cart-empty');
      const cartFooter = document.getElementById('cart-footer');
      const cartTotal = document.getElementById('cart-total');
      const continueShoppingBtn = document.getElementById('continue-shopping');
      const viewCartBtn = document.getElementById('view-cart');
      const checkoutBtn = document.getElementById('checkout-btn');

      // Cart drawer open/close functions
      function openCartDrawer() {
        cartDrawer.classList.remove('opacity-0', 'invisible');
        const slidePanel = cartDrawer.querySelector('div.transform');
        if (slidePanel) {
          setTimeout(() => {
            slidePanel.classList.remove('translate-x-full');
          }, 10);
        }
        loadCartData();
      }

      function closeCartDrawer() {
        const slidePanel = cartDrawer.querySelector('div.transform');
        if (slidePanel) {
          slidePanel.classList.add('translate-x-full');
        }
        setTimeout(() => {
          cartDrawer.classList.add('opacity-0', 'invisible');
        }, 300);
      }

      // Load cart data from Shopify
      async function loadCartData() {
        try {
          const response = await fetch('/cart.js');
          const cart = await response.json();
          updateCartDisplay(cart);
        } catch (error) {
          console.error('Error loading cart:', error);
        }
      }

      // Update cart display
      function updateCartDisplay(cart) {
        const itemCount = cart.item_count;

        // Update cart count badge
        if (itemCount > 0) {
          cartCount.textContent = itemCount;
          cartCount.classList.remove('hidden');
        } else {
          cartCount.classList.add('hidden');
        }

        // Show/hide empty state
        if (cart.items.length === 0) {
          cartEmpty.classList.remove('hidden');
          cartItemsContainer.classList.add('hidden');
          cartFooter.classList.add('hidden');
        } else {
          cartEmpty.classList.add('hidden');
          cartItemsContainer.classList.remove('hidden');
          cartFooter.classList.remove('hidden');

          // Render cart items
          renderCartItems(cart.items);

          // Update total
          cartTotal.textContent = formatMoney(cart.total_price);
        }
      }

      // Render cart items
      function renderCartItems(items) {
        const cartItemsHtml = items
          .map(
            (item) => `
          <div class="flex items-center gap-4 p-4 border-b border-border" data-key="${item.key}">
            <div class="w-16 h-16 bg-muted rounded-md overflow-hidden flex-shrink-0">
              ${
                item.image
                  ? `<img src="${item.image}" alt="${item.product_title}" class="w-full h-full object-cover">`
                  : '<div class="w-full h-full bg-muted"></div>'
              }
            </div>
            <div class="flex-1 min-w-0">
              <h4 class="font-medium text-sm truncate">${item.product_title}</h4>
              ${item.variant_title ? `<p class="text-xs text-muted-foreground">${item.variant_title}</p>` : ''}
              <div class="flex items-center gap-2 mt-1">
                <span class="text-sm font-medium">${formatMoney(item.final_price)}</span>
                ${
                  item.original_price > item.final_price
                    ? `<span class="text-xs text-muted-foreground line-through">${formatMoney(
                        item.original_price
                      )}</span>`
                    : ''
                }
              </div>
            </div>
            <div class="flex items-center gap-2">
              <button class="quantity-btn" data-key="${item.key}" data-action="decrease">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 12H4"></path>
                </svg>
              </button>
              <span class="w-8 text-center text-sm">${item.quantity}</span>
              <button class="quantity-btn" data-key="${item.key}" data-action="increase">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                </svg>
              </button>
            </div>
            <button class="remove-item text-muted-foreground hover:text-destructive" data-key="${item.key}">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
            </button>
          </div>
        `
          )
          .join('');

        cartItemsContainer.innerHTML = cartItemsHtml;

        // Add event listeners for quantity and remove buttons
        addCartItemEventListeners();
      }

      // Add event listeners for cart item actions
      function addCartItemEventListeners() {
        // Quantity buttons
        document.querySelectorAll('.quantity-btn').forEach((btn) => {
          btn.addEventListener('click', async (e) => {
            const key = e.currentTarget.dataset.key;
            const action = e.currentTarget.dataset.action;
            const currentItem = document.querySelector(`[data-key="${key}"]`);
            const currentQty = parseInt(currentItem.querySelector('span').textContent);

            let newQty = action === 'increase' ? currentQty + 1 : currentQty - 1;
            if (newQty < 0) newQty = 0;

            await updateCartItem(key, newQty);
          });
        });

        // Remove buttons
        document.querySelectorAll('.remove-item').forEach((btn) => {
          btn.addEventListener('click', async (e) => {
            const key = e.currentTarget.dataset.key;
            await updateCartItem(key, 0);
          });
        });
      }

      // Update cart item quantity
      async function updateCartItem(key, quantity) {
        try {
          const response = await fetch('/cart/change.js', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({
              id: key,
              quantity: quantity,
            }),
          });

          if (response.ok) {
            const cart = await response.json();
            updateCartDisplay(cart);
          }
        } catch (error) {
          console.error('Error updating cart:', error);
        }
      }

      // Format money helper
      function formatMoney(cents) {
        return (cents / 100).toLocaleString('en-US', {
          style: 'currency',
          currency: 'USD',
        });
      }

      // Event listeners
      if (cartTrigger) {
        cartTrigger.addEventListener('click', openCartDrawer);
      }

      if (cartDrawerClose) {
        cartDrawerClose.addEventListener('click', closeCartDrawer);
      }

      if (continueShoppingBtn) {
        continueShoppingBtn.addEventListener('click', closeCartDrawer);
      }

      if (viewCartBtn) {
        viewCartBtn.addEventListener('click', () => {
          window.location.href = '/cart';
        });
      }

      if (checkoutBtn) {
        checkoutBtn.addEventListener('click', () => {
          window.location.href = '/checkout';
        });
      }

      // Close cart drawer when clicking outside
      if (cartDrawer) {
        cartDrawer.addEventListener('click', (e) => {
          if (e.target === cartDrawer) {
            closeCartDrawer();
          }
        });
      }

      // Load initial cart data
      loadCartData();

      // Listen for cart updates from other parts of the site
      document.addEventListener('cart:updated', loadCartData);

      // Escape key handling
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') {
          // Close mobile menu
          if (mobileMenu && !mobileMenu.classList.contains('invisible')) {
            const slidePanel = mobileMenu.querySelector('div.transform');
            if (slidePanel) {
              slidePanel.classList.add('translate-x-full');
            }
            setTimeout(() => {
              mobileMenu.classList.add('opacity-0', 'invisible');
            }, 300);
          }

          // Close cart drawer
          if (cartDrawer && !cartDrawer.classList.contains('invisible')) {
            closeCartDrawer();
          }
        }
      });
    });
  </script>
</{{ header_tag }}>

{%- if settings.cart_type == 'notification' -%}
  {%- render 'cart-notification',
    color_scheme: section.settings.color_scheme,
    desktop_menu_type: section.settings.menu_type_desktop
  -%}
{%- endif -%}

{% javascript %}
  class StickyHeader extends HTMLElement {
    constructor() {
      super();
    }

    connectedCallback() {
      this.header = document.querySelector('.section-header');
      this.headerIsAlwaysSticky =
        this.getAttribute('data-sticky-type') === 'always' ||
        this.getAttribute('data-sticky-type') === 'reduce-logo-size';
      this.headerBounds = {};

      this.setHeaderHeight();

      window.matchMedia('(max-width: 990px)').addEventListener('change', this.setHeaderHeight.bind(this));

      if (this.headerIsAlwaysSticky) {
        this.header.classList.add('shopify-section-header-sticky');
      }

      this.currentScrollTop = 0;
      this.preventReveal = false;
      this.predictiveSearch = this.querySelector('predictive-search');

      this.onScrollHandler = this.onScroll.bind(this);
      this.hideHeaderOnScrollUp = () => (this.preventReveal = true);

      this.addEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.addEventListener('scroll', this.onScrollHandler, false);

      this.createObserver();
    }

    setHeaderHeight() {
      document.documentElement.style.setProperty('--header-height', `${this.header.offsetHeight}px`);
    }

    disconnectedCallback() {
      this.removeEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.removeEventListener('scroll', this.onScrollHandler);
    }

    createObserver() {
      let observer = new IntersectionObserver((entries, observer) => {
        this.headerBounds = entries[0].intersectionRect;
        observer.disconnect();
      });

      observer.observe(this.header);
    }

    onScroll() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (this.predictiveSearch && this.predictiveSearch.isOpen) return;

      if (scrollTop > this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (this.preventHide) return;
        requestAnimationFrame(this.hide.bind(this));
      } else if (scrollTop < this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (!this.preventReveal) {
          requestAnimationFrame(this.reveal.bind(this));
        } else {
          window.clearTimeout(this.isScrolling);

          this.isScrolling = setTimeout(() => {
            this.preventReveal = false;
          }, 66);

          requestAnimationFrame(this.hide.bind(this));
        }
      } else if (scrollTop <= this.headerBounds.top) {
        this.header.classList.remove('scrolled-past-header');
        requestAnimationFrame(this.reset.bind(this));
      }

      this.currentScrollTop = scrollTop;
    }

    hide() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-hidden', 'shopify-section-header-sticky');
      this.closeMenuDisclosure();
      this.closeSearchModal();
    }

    reveal() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-sticky', 'animate');
      this.header.classList.remove('shopify-section-header-hidden');
    }

    reset() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.remove('shopify-section-header-hidden', 'shopify-section-header-sticky', 'animate');
    }

    closeMenuDisclosure() {
      this.disclosures = this.disclosures || this.header.querySelectorAll('header-menu');
      this.disclosures.forEach((disclosure) => disclosure.close());
    }

    closeSearchModal() {
      this.searchModal = this.searchModal || this.header.querySelector('details-modal');
      this.searchModal.close(false);
    }
  }

  customElements.define('sticky-header', StickyHeader);
{% endjavascript %}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tiktok_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "max_blocks": 3,
  "settings": [
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.header.settings.logo_position.options__4.label"
        }
      ],
      "default": "middle-left",
      "label": "t:sections.header.settings.logo_position.label",
      "info": "t:sections.header.settings.logo_help.content"
    },
    {
      "type": "select",
      "id": "mobile_logo_position",
      "options": [
        {
          "value": "center",
          "label": "t:sections.header.settings.mobile_logo_position.options__1.label"
        },
        {
          "value": "left",
          "label": "t:sections.header.settings.mobile_logo_position.options__2.label"
        }
      ],
      "default": "center",
      "label": "t:sections.header.settings.mobile_logo_position.label"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "select",
      "id": "menu_type_desktop",
      "options": [
        {
          "value": "dropdown",
          "label": "t:sections.header.settings.menu_type_desktop.options__1.label"
        },
        {
          "value": "mega",
          "label": "t:sections.header.settings.menu_type_desktop.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.settings.menu_type_desktop.options__3.label"
        }
      ],
      "default": "dropdown",
      "label": "t:sections.header.settings.menu_type_desktop.label"
    },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        },
        {
          "value": "reduce-logo-size",
          "label": "t:sections.header.settings.sticky_header_type.options__4.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "Mega Menu Settings"
    },
    {
      "type": "text",
      "id": "hotel_collection_title",
      "label": "Hotel Collection Title",
      "default": "Hotel Collection"
    },
    {
      "type": "textarea",
      "id": "hotel_collection_links",
      "label": "Hotel Collection Links",
      "info": "Format: Name|URL,Name|URL (e.g., Calm Down|/collections/calm-down,Zest & The City|/collections/zest-the-city)",
      "default": "All Products|/collections/all-products,Calm Down|/collections/calm-down,Zest & The City|/collections/zest-the-city,Peach Tease|/collections/peach-tease,Mystic Nights|/collections/mystic-nights,Blooming Flirt|/collections/blooming-flirt,Wet Bar|/collections/wet-bar"
    },
    {
      "type": "text",
      "id": "gilded_collection_title",
      "label": "Gilded Collection Title",
      "default": "Gilded Exotics"
    },
    {
      "type": "textarea",
      "id": "gilded_collection_links",
      "label": "Gilded Collection Links",
      "info": "Format: Name|URL,Name|URL (e.g., On Par|/collections/on-par,Sweet Juliet|/collections/sweet-juliet)",
      "default": "On Par|/collections/on-par,Sweet Juliet|/collections/sweet-juliet,Juicy Theory|/collections/juicy-theory,Portable Units|/collections/portable-diffusers"
    },
    {
      "type": "text",
      "id": "popular_collection_title",
      "label": "Popular Collection Title",
      "default": "Popular Collections"
    },
    {
      "type": "textarea",
      "id": "popular_collection_links",
      "label": "Popular Collection Links",
      "info": "Format: Name|URL,Name|URL (e.g., Best Sellers|/collections/best-sellers,New Arrivals|/collections/new-arrivals)",
      "default": "Best Sellers|/collections/best-sellers,New Arrivals|/collections/new-arrivals,Bundle Deals|/collections/bundles-deals,Starter Kits|/collections/starter-kits"
    },
    {
      "type": "header",
      "content": "Bundles Menu Settings"
    },
    {
      "type": "text",
      "id": "bundle_types_title",
      "label": "Bundle Types Title",
      "default": "Bundle Types"
    },
    {
      "type": "textarea",
      "id": "bundle_types_links",
      "label": "Bundle Types Links",
      "info": "Format: Name|URL,Name|URL (e.g., Starter Bundles|/collections/starter-bundles,Home Bundles|/collections/home-bundles)",
      "default": "Starter Bundles|/collections/starter-bundles,Home Bundles|/collections/home-bundles,Car Bundles|/collections/car-bundles,Gift Bundles|/collections/gift-bundles"
    },
    {
      "type": "text",
      "id": "special_offers_title",
      "label": "Special Offers Title",
      "default": "Special Offers"
    },
    {
      "type": "textarea",
      "id": "special_offers_links",
      "label": "Special Offers Links",
      "info": "Format: Name|URL,Name|URL (e.g., Flash Deals|/collections/flash-deals,Seasonal Bundles|/collections/seasonal-bundles)",
      "default": "Flash Deals|/collections/flash-deals,Seasonal Bundles|/collections/seasonal-bundles,Bulk Deals|/collections/bulk-deals,Subscription Deals|/collections/subscription-deals"
    },
    {
      "type": "header",
      "content": "Featured Cards Settings"
    },
    {
      "type": "text",
      "id": "featured_card1_url",
      "label": "Featured Card 1 URL",
      "default": "/collections/best-sellers",
      "info": "Enter the URL path (e.g., /collections/best-sellers)"
    },
    {
      "type": "text",
      "id": "featured_card1_category",
      "label": "Featured Card 1 Category",
      "default": "Best Sellers"
    },
    {
      "type": "text",
      "id": "featured_card1_title",
      "label": "Featured Card 1 Title",
      "default": "Top Scents"
    },
    {
      "type": "text",
      "id": "featured_card1_description",
      "label": "Featured Card 1 Description",
      "default": "Most loved fragrances"
    },
    {
      "type": "text",
      "id": "featured_card2_url",
      "label": "Featured Card 2 URL",
      "default": "/collections/bundles-deals",
      "info": "Enter the URL path (e.g., /collections/bundles-deals)"
    },
    {
      "type": "text",
      "id": "featured_card2_category",
      "label": "Featured Card 2 Category",
      "default": "Save More"
    },
    {
      "type": "text",
      "id": "featured_card2_title",
      "label": "Featured Card 2 Title",
      "default": "Bundle Deals"
    },
    {
      "type": "text",
      "id": "featured_card2_description",
      "label": "Featured Card 2 Description",
      "default": "Complete scent packages"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__1.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "menu_color_scheme",
      "label": "t:sections.header.settings.menu_color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__utilities.content"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_country_selector.label",
      "info": "t:sections.header.settings.enable_country_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_language_selector.label",
      "info": "t:sections.header.settings.enable_language_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_customer_avatar",
      "default": true,
      "label": "t:sections.header.settings.enable_customer_avatar.label",
      "info": "t:sections.header.settings.enable_customer_avatar.info"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.header.settings.margin_bottom.label",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "@app"
    }
  ]
}
{% endschema %}
